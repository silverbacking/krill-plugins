# Krill App - Agent Discovery & Verification

## Descobriment d'Agents

Quan l'usuari obre Krill App:

```
┌─────────────────────────────────────────────────────────────────┐
│  Krill App - Agents Disponibles                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📡 Servidor: matrix.silverbacking.ai                           │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🤖 Jarvis                              [✅ Paired]      │   │
│  │  Personal AI assistant                                   │   │
│  │  Gateway: clawdbot-001 ✓ Verified                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🤖 TecBot                              [➕ Pair]        │   │
│  │  Assistente académico del Tec                            │   │
│  │  Gateway: tec-gateway ✓ Verified                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🤖 SuspiciousBot                       [⚠️ Unverified] │   │
│  │  Too good to be true                                     │   │
│  │  Gateway: fake-gateway ✗ Verification failed             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Flux de Descobriment

```
┌─────────────┐                    ┌─────────────────┐
│  Krill App  │                    │  KrillMatrix    │
│             │                    │    Server       │
└──────┬──────┘                    └────────┬────────┘
       │                                    │
       │  1. Join room de registre          │
       │     #krill-agents:server           │
       │ ─────────────────────────────────► │
       │                                    │
       │  2. GET /rooms/{id}/state          │
       │ ─────────────────────────────────► │
       │                                    │
       │  3. State events (ai.krill.agent)  │
       │ ◄───────────────────────────────── │
       │                                    │
       │  4. Per cada agent:                │
       │     → Verificar amb gateway        │
       │     → Comprovar si ja paired       │
       │                                    │
```

## Validació del Hash

Per cada agent trobat, Krill App verifica amb el gateway:

```
┌─────────────┐                              ┌─────────────────┐
│  Krill App  │                              │  Gateway        │
│             │                              │  (gateway_url)  │
└──────┬──────┘                              └────────┬────────┘
       │                                              │
       │  POST /krill/verify                          │
       │  {                                           │
       │    "agent_mxid": "@jarvis:matrix...",        │
       │    "gateway_id": "clawdbot-001",             │
       │    "verification_hash": "a3f2b1c4...",       │
       │    "enrolled_at": 1706817600                 │
       │  }                                           │
       │ ───────────────────────────────────────────► │
       │                                              │
       │    Gateway recalcula:                        │
       │    expected = HMAC(secret, mxid|id|ts)       │
       │    compara amb hash rebut                    │
       │                                              │
       │  Response                                    │
       │  { "valid": true, "status": "online" }       │
       │ ◄─────────────────────────────────────────── │
       │                                              │
```

## Estats d'un Agent a Krill App

| Estat | Significat | Acció disponible |
|-------|------------|------------------|
| ✅ **Paired + Verified** | Hash vàlid, ja emparellat | Xatejar, configurar senses |
| ➕ **Verified, not paired** | Hash vàlid, no emparellat | Iniciar pairing |
| ⚠️ **Unverified** | Hash invàlid o gateway no respon | Mostrar warning, no permetre pairing |
| 🔴 **Gateway offline** | No es pot contactar gateway | Indicar que l'agent pot no estar disponible |

## Verificació - Quan es fa?

1. **Al descobrir**: Primer cop que l'app veu l'agent
2. **Periòdicament**: Cada X hores per confirmar que segueix vàlid
3. **Abans del pairing**: Sempre verificar just abans d'emparellar
4. **Cache**: Guardar resultat amb TTL (ex: 1 hora)

## Què passa si la verificació falla?

```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️  Agent no verificat                                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  No s'ha pogut verificar "SuspiciousBot"                        │
│                                                                 │
│  Possibles causes:                                              │
│  • El gateway no respon                                         │
│  • L'agent ja no està registrat al gateway                      │
│  • L'event ha estat manipulat                                   │
│                                                                 │
│  [Reintentar]  [Ignorar agent]                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Room de Registre - Com la troba l'App?

### Opció A: Alias conegut
El servidor KrillMatrix té una room amb alias estàndard:
```
#krill-agents:matrix.silverbacking.ai
```

### Opció B: Well-known endpoint
El servidor exposa un endpoint:
```
GET /.well-known/krill
{
  "agents_room": "!abc123:matrix.silverbacking.ai",
  "version": "1.0"
}
```

### Opció C: Múltiples gateways
Si hi ha múltiples gateways, cada un té la seva room:
```
#krill-agents-clawdbot:matrix.silverbacking.ai
#krill-agents-tecbot:matrix.silverbacking.ai
```
I el well-known retorna un array:
```json
{
  "agents_rooms": [
    { "gateway": "clawdbot", "room": "!abc..." },
    { "gateway": "tecbot", "room": "!def..." }
  ]
}
```

## Diagrama Complet

```
┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Krill App  │     │  KrillMatrix    │     │  OpenClaw       │
│  (mòbil)    │     │  Server         │     │  Gateway        │
└──────┬──────┘     └────────┬────────┘     └────────┬────────┘
       │                     │                       │
       │  1. GET /.well-known/krill                  │
       │ ──────────────────► │                       │
       │                     │                       │
       │  2. { agents_room } │                       │
       │ ◄────────────────── │                       │
       │                     │                       │
       │  3. Join room       │                       │
       │ ──────────────────► │                       │
       │                     │                       │
       │  4. GET state       │                       │
       │ ──────────────────► │                       │
       │                     │                       │
       │  5. [agents]        │                       │
       │ ◄────────────────── │                       │
       │                     │                       │
       │  6. POST /krill/verify (per agent)          │
       │ ──────────────────────────────────────────► │
       │                     │                       │
       │  7. { valid: true } │                       │
       │ ◄────────────────────────────────────────── │
       │                     │                       │
       │  8. Mostrar agents  │                       │
       │     amb estat       │                       │
       │                     │                       │
```
